[user]
	name = Ariel Flesler
	email = aflesler@gmail.com
[github]
  user = flesler
[alias]
	s = status -s
	d = diff
	a = add -A
	undo = reset --soft HEAD^
	discard = reset --hard HEAD^
	co = checkout
	rev = checkout --
	p = push
	pf = push -f
	pr = pull --rebase
	st = add -A && stash
	pop = stash pop
	stpll = add -A && stash && pull --rebase && stash pop
	br = branch
	rb = rebase
	rbc = add -A && rebase --continue
	rba = rebase --abort
	rbm = rebase master
	lg = log --graph --pretty=format:'%Cred%h%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
[core]
	# Use custom `.gitignore`
	excludesfile = ~/.gitignore
	autocrlf = false
	quotepath = false
	editor = subl
	ignorecase = false
[color]
	# Use colors in Git commands that are capable of colored output when
	ui = auto
[push]
	# Make `git push` push relevant annotated tags when pushing branches out.
	followTags = true
	default = matching
[pull]
	rebase = true
[diff]
	# Detect copies as well as renames
	renames = copies
[rerere]
	enabled = 1
[help]
	# Automatically correct and execute mistyped commands
	autocorrect = 1